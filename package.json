{
  "name": "sfnc-share-links",
  "version": "1.0.0",
  "private": true,
  "description": "Outlook (Classic) add-in for OneDrive/SharePoint sharing links - create, expire, manage access, convert attachments",
  "scripts": {
    "certs": "office-addin-dev-certs install",
    "start": "node server.js",
    "validate": "office-addin-manifest validate manifest.xml"
  },
  "dependencies": {
    "@azure/msal-browser": "^3.26.0"
  },
  "devDependencies": {
    "office-addin-dev-certs": "latest",
    "office-addin-manifest": "latest"
  }
}
