node_modules/
package-lock.json

# manifest.xml is generated from manifest.template.xml by the setup script,
# stamped with the deployer's hosting URL. Don't commit it -- each fork has
# its own.
manifest.xml

# config.js holds the Entra clientId/tenantId. These are not secrets for a
# public-client SPA (no client secret exists), so it IS committed to ship
# with the GitHub Pages deploy.
