node_modules/
package-lock.json

# config.js holds the Entra clientId/tenantId. These are NOT secrets for a
# public client SPA (no client secret exists; tenantId is derivable from the
# email domain), so it IS committed here to ship with the GitHub Pages deploy.
